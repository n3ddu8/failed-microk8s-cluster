all:
  children:
    master:
      hosts:
        <hostname-of-pi>:
          ansible_host:
          ansible_user:
          ansible_become: true
          ansible_become_password:
    workers:
      hosts:
        <hostname-of-pi>:
          ansible_host:
          ansible_user:
          ansible_become: true
          ansible_become_password:
        <hostname-of-pi>:
          ansible_host:
          ansible_user:
          ansible_become: true
          ansible_become_password:

